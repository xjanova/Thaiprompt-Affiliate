<?php

/**
 * POS System Configuration
 *
 * การตั้งค่าสำหรับระบบ POS Terminal
 */

return [

    /*
    |--------------------------------------------------------------------------
    | Secret Key สำหรับเข้ารหัส/ถอดรหัส Product Key
    |--------------------------------------------------------------------------
    |
    | ⚠️ สำคัญ: ต้องกำหนดใน .env และห้าม commit ค่าจริงลง repository!
    |
    | ใช้สำหรับ:
    | - XOR encryption/decryption ของ Product Key
    | - Hash เพื่อสร้าง device_id
    |
    */
    'secret_key' => env('POS_SECRET_KEY', ''),

    /*
    |--------------------------------------------------------------------------
    | API Rate Limiting
    |--------------------------------------------------------------------------
    |
    | จำกัดจำนวน request ต่อนาทีสำหรับ POS API
    |
    */
    'rate_limit' => env('POS_RATE_LIMIT', 60),

    /*
    |--------------------------------------------------------------------------
    | Session Timeout (minutes)
    |--------------------------------------------------------------------------
    |
    | ระยะเวลาที่ POS Terminal session หมดอายุ
    |
    */
    'session_timeout' => env('POS_SESSION_TIMEOUT', 480), // 8 ชั่วโมง

    /*
    |--------------------------------------------------------------------------
    | Auto Sync Interval (minutes)
    |--------------------------------------------------------------------------
    |
    | ระยะเวลาที่ POS จะ sync ข้อมูลอัตโนมัติ
    |
    */
    'sync_interval' => env('POS_SYNC_INTERVAL', 15),

    /*
    |--------------------------------------------------------------------------
    | Max Offline Days
    |--------------------------------------------------------------------------
    |
    | จำนวนวันที่ POS สามารถทำงาน offline ได้
    | หลังจากนั้นต้อง sync กับ server
    |
    */
    'max_offline_days' => env('POS_MAX_OFFLINE_DAYS', 7),

];
