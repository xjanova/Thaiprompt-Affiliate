/**
 * Cart Placeholder - ไฟล์ placeholder สำหรับปุ่มตะกร้ากลาง
 * หน้านี้จะไม่ถูกใช้งานจริง เนื่องจาก tabBarButton จะ redirect ไปที่ /cart
 */

import { View } from 'react-native';

export default function CartPlaceholder() {
  return <View />;
}
