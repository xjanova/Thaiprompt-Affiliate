<?php

return [

    /*
    |--------------------------------------------------------------------------
    | เกราะกัน Hotlink Protection ของ Cloudflare (รูปที่ส่งเข้า LINE)
    |--------------------------------------------------------------------------
    |
    | 🐛 เคสจริง 2026-09-19: "รูปไพ่และอื่นๆ ใน LINE ไม่ขึ้นอีกแล้ว"
    |   อาการแยกได้คมมากจากคำบอกของเจ้าของ — **ในแอพ LINE เห็น แต่บนเว็บ OA ไม่เห็น**
    |   และ **ไพ่ของบิล 39 เห็น แต่ของ Celtic 99 ไม่เห็น**
    |
    | สาเหตุ: Cloudflare Hotlink Protection (error code 1011) เปิดอยู่ที่โซน
    |   - แอพ LINE ดึงรูปโดย "ไม่มี Referer" → ผ่าน 200
    |   - เว็บ OA (chat.line.biz) ดึงโดยเบราว์เซอร์ที่ "แนบ Referer" → 403
    |   - Cloudflare กันเฉพาะนามสกุล jpg/jpeg/png/gif/ico — **ไม่กิน .webp**
    |     ⇒ ไพ่ 39 ส่ง .webp ตรง ๆ จึงรอด ส่วน Celtic 99 วิ่งผ่านตัวแปลง
    |       WebP→JPEG (ใส่ไว้ 2026-07-25) จึงกลายเป็น .jpg แล้วโดนบล็อก
    |
    | เกราะนี้ทำกลับด้านกับของเดิม: แปลง jpg/png/gif ที่เราโฮสต์เอง → .webp
    | แล้ว cache ถาวร เพื่อให้รอดด่าน Hotlink ทั้งผังดวง QR และการ์ดแพคเกจ
    |
    | ⚠️ นี่คือทางอ้อม ไม่ใช่การแก้ต้นเหตุ — ต้นเหตุอยู่ที่ Cloudflare
    |   (Scrape Shield → Hotlink Protection → ปิด หรือยกเว้น path /storage/*)
    |   เมื่อแก้ที่ Cloudflare แล้ว ปิดเกราะนี้ได้ด้วย LINE_WEBP_SHIELD=false
    |
    */

    'webp_shield' => env('LINE_WEBP_SHIELD', true),

    /*
    |--------------------------------------------------------------------------
    | คุณภาพไฟล์ WebP ที่แปลง
    |--------------------------------------------------------------------------
    |
    | 90 = คมพอสำหรับ QR code และตัวหนังสือบนผังดวง แต่ไฟล์ยังเล็กกว่า JPEG เดิม
    |
    */

    'webp_quality' => (int) env('LINE_WEBP_QUALITY', 90),

];
