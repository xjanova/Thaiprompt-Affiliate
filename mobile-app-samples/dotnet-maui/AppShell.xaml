<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ThaipromptAffiliate.Views"
       x:Class="ThaipromptAffiliate.AppShell"
       Shell.FlyoutBehavior="Disabled"
       Shell.NavBarIsVisible="False"
       Shell.TabBarIsVisible="False">

    <!--
        AppShell - ระบบ Navigation หลักของแอพ

        Flow การใช้งาน:
        1. SplashPage → โหลดแอพ, ตรวจสอบ auth
        2. HubSelectionPage → เลือกบริการที่ต้องการ
        3. LoginPage → เข้าสู่ระบบ (ถ้ายังไม่ login)
        4. DashboardPage → หน้าหลักหลัง login
        5. อื่นๆ → หน้าบริการต่างๆ

        หมายเหตุ:
        - ซ่อน Flyout, NavBar, TabBar เพราะใช้ custom navigation
        - ทุกหน้าลงทะเบียนเป็น route ใน code-behind
    -->

    <!-- หน้าเริ่มต้น: Splash Screen -->
    <ShellContent
        Route="splash"
        ContentTemplate="{DataTemplate views:SplashPage}" />

</Shell>
